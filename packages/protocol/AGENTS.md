# Protocol

This dependency-free package is the source of truth for runtime-independent contracts shared across processes and UIs. Evolve wire formats additively; breaking changes require explicit versioning.
